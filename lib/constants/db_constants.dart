class DBConstants {
  DBConstants._();

  // DB Name
  static const DB_NAME = 'Simple.db';

  // Store Name
  static const String AUTH_STORE_NAME = 'auth';

  // Fields
  static const FIELD_ID = 'id';
}
